spring:
  application:
    name: participant-service-registracion-fila

  datasource:
    url: jdbc:postgresql://localhost:5432/rpp
    username: postgres
    password: Pedro2012=
    driver-class-name: org.postgresql.Driver

  jpa:
    show-sql: true
    open-in-view: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true

  rabbitmq:
    listener:
      simple:
        default-requeue-rejected: false
        retry:
          enabled: true
          max-attempts: 3
    host: localhost
    port: 5672
    username: guest
    password: guest

server:
  port: 8084